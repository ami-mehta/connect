<html>
  <head>
    <link rel="stylesheet" href="style.css" />
    <title>Curmudgeons Anonymous</title>
    <script src="https://cdn.jsdelivr.net/npm/p5@1.3.0/lib/p5.js"></script>
    <script>
      let myObj = {};
      let bgColor;

      // let inconsolata;
      // function preload() {
      //   inconsolata = loadFont("assets/inconsolata-black.otf");
      // }

      function setup() {
        createCanvas(windowWidth, windowHeight); //Add WebGL?
         myObj = <%- JSON.stringify(data) %>;
        // myObj = JSON.parse(myVar);
        bgColor = color( random(255), random(255), random(255) );
      }

      function draw() {
        background(bgColor);
        // colorMode(HSB, 360, 100, 100);
        frameRate(1);
        var mysomething = random(myObj);
        console.log(mysomething);
        var answer = mysomething.answer1;
        text(
          //random(myVar)[`answer${Math.floor(random(1, 5))}`],
          answer,
          random(width),
          random(height)
          // rotateX(random),
          // rotateY(random)
        );
      }
    </script>
    <body>
      <!-- <% if (data.length > 0) { for (var i = 0; i < data.length; i++) { %>
      <div class="dataout" id="dataout<%= i %>">
        <a href="/displayrecord?_id=<%= data[i]._id%>">
          <%= data[i].answer1 %> <%= data[i].answer2 %> <%= data[i].answer3 %>
          <%= data[i].answer4 %>
        </a>
      </div>
      <% }} %> -->
    </body>
  </head>
</html>
